---
title: "Data Analysis"
output: html_document
---

```{r upload-data}
Summer_olympics <- read_csv("Summer-Olympic-medals-1976-to-2008.csv")
Country_codes <- read_csv("coutry-and-continent-codes.csv")
```

```{r augmenting-data}
#names(Country_codes)[names(Country_codes) == 'Three_Letter_Country_Code'] <- 'Country_code'
Olympic_final <- Summer_olympics %>%
  left_join(Country_codes, by = c("Country_Code" = "Three_Letter_Country_Code")) %>% mutate(Medal_value = case_when(
      Medal == "Gold" ~ 3,
      Medal == "Silver" ~ 2,
      Medal == "Bronze" ~ 1))
```

```{r}
Olympic_final %>%
   filter(Continent_Name == "North America") %>%
  ggplot(aes(x = Country, y = Medal_value, fill=Medal)) +
  geom_col()  +
  facet_wrap(~ "Continent_Name")
```

```{r}
Olympic_final %>%
<<<<<<< HEAD
  group_by(Country, total_medal) %>%
  top_n(n = 5, Country) %>%
  filter(Continent_Name == "Europe") %>%
  ggplot(aes(x = Country, y = Medal_value, fill=Medal)) +
=======
  group_by(Country, Medal_value) %>%
  mutate(Total_medals = sum(Medal_value),
         Country = as.factor(Country)) %>%
  filter(Continent_Name == "Europe", Total_medals > 100) %>%
  ggplot(aes(x = fct_reorder(Country, Total_medals), y =  Medal_value,    fill = fct_relevel(Medal, c("Gold", "Silver", "Bronze")))) +
>>>>>>> 6cfa8be7a5555b9bcbf0116633670efcd67c8afc
  geom_col()  +
  facet_wrap(~ "Continent_Name") + 
  coord_flip()
  
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
